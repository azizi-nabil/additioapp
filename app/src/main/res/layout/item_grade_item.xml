<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="?attr/colorSurface">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Icon -->
        <ImageView
            android:id="@+id/imgGradeIcon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_assignment"
            app:tint="?attr/colorPrimary"
            android:background="@drawable/bg_circle_primary_container"
            android:padding="8dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintVertical_bias="0.0"/>

        <!-- Name -->
        <TextView
            android:id="@+id/textGradeItemName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="8dp"
            android:text="Midterm Exam"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="?android:attr/textColorPrimary"
            app:layout_constraintStart_toEndOf="@id/imgGradeIcon"
            app:layout_constraintTop_toTopOf="@id/imgGradeIcon"
            app:layout_constraintEnd_toStartOf="@id/btnMoreOptions" />

        <!-- Date -->
        <TextView
            android:id="@+id/textGradeItemDate"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Oct 24, 2023"
            android:textSize="14sp"
            android:textColor="?attr/colorOnSurfaceVariant"
            app:layout_constraintStart_toStartOf="@id/textGradeItemName"
            app:layout_constraintTop_toBottomOf="@id/textGradeItemName"
            app:layout_constraintEnd_toStartOf="@id/btnMoreOptions" />

        <!-- Details Container -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="8dp"
            app:layout_constraintStart_toStartOf="@id/textGradeItemName"
            app:layout_constraintTop_toBottomOf="@id/textGradeItemDate"
            app:layout_constraintEnd_toStartOf="@id/btnMoreOptions"
            app:layout_constraintBottom_toBottomOf="parent">

            <TextView
                android:id="@+id/textGradeItemCategory"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Exam"
                android:textSize="12sp"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:background="?attr/colorSurfaceVariant"
                android:paddingHorizontal="8dp"
                android:paddingVertical="2dp"
                android:layout_marginEnd="8dp"/>

            <TextView
                android:id="@+id/textGradeItemMaxScore"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Max: 100"
                android:textSize="12sp"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:textStyle="bold"
                android:layout_marginEnd="8dp"/>

            <!-- Calculated Indicator -->
            <TextView
                android:id="@+id/textCalculatedIndicator"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Σ Calculated"
                android:textSize="11sp"
                android:textColor="#7B1FA2"
                android:background="#F3E5F5"
                android:paddingHorizontal="8dp"
                android:paddingVertical="2dp"
                android:visibility="gone"/>
        </LinearLayout>

        <!-- More Options -->
        <ImageView
            android:id="@+id/btnMoreOptions"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_more_vert_24dp"
            app:tint="?attr/colorOnSurfaceVariant"
            android:padding="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
